/** @import { PageFunction } from '@domstack/static' */

export const vars = {
  title: 'Projects',
  layout: 'blog-auto-index',
  noindex: true
}

/**
 * @type {PageFunction<{
 *       title: string
 *       publishDate: string
 * }>}
 */
export default function projectIndex () {
  return ''
}
